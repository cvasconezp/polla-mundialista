# --- Base de datos (Railway: copia la "Postgres Connection URL") ---
DATABASE_URL="postgresql://user:pass@host:port/railway"

# --- NextAuth ---
NEXTAUTH_URL="http://localhost:3000"        # en prod: https://tudominio.vercel.app
NEXTAUTH_SECRET="genera-uno-con: openssl rand -base64 32"

# --- Login con Google (Google Cloud Console > OAuth 2.0) ---
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# --- Quién es admin (emails separados por coma) ---
ADMIN_EMAILS="cvasconezp@gmail.com"

# --- API de resultados (football-data.org > Free tier incluye World Cup) ---
FOOTBALL_DATA_TOKEN=""

# --- Secreto para proteger el endpoint del cron ---
CRON_SECRET="genera-otro-con: openssl rand -base64 32"
